# Legacy Compatibility Note

The canonical repository operating rules now live in `AGENTS.md`.

Use `AGENTS.md` for current instructions, `GOAL.md` for mission and priority order, and `TASKS.md` for live execution state.
